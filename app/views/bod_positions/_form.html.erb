<%= form_for(@bod_position) do |f| %>
  <% if @bod_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bod_position.errors.count, "error") %> prohibited this bod_position from being saved:</h2>

      <ul>
      <% @bod_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
